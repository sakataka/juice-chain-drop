# Agent Instructions for Juice Chain Drop

このリポジトリは、生成AIエージェントが継続的に機能追加・改善することを前提にしたWebゲームです。人間向けの読みやすさよりも、機能追加時の変更範囲を小さくし、バグを混入しにくい構造を優先してください。

## 設計原則

- ゲームルールは、描画、音、DOM、ブラウザイベント、永続化などの副作用から分離する。
- 操作は直接状態を書き換えるのではなく、明確なコマンドとして扱う。
- コマンドの結果は、状態変化、音、視覚演出、表示更新などの意図を分けて表現する。
- 副作用の実行は境界層に寄せ、ルールや進行管理の中から具体的な描画API、音API、DOM APIを直接呼ばない。
- 新しい仕様は、既存の責務境界に沿って追加し、巨大な単一ファイルや多目的クラスへ戻さない。
- 同じ仕様を複数箇所に重複実装しない。入力、表示、演出は共通のゲーム状態やコマンド結果から派生させる。
- 分岐が増える仕様は、可能な限り定義テーブルや小さなハンドラーに寄せ、追加時に触る場所を限定する。

## 実装方針

- まずゲームとしての真実を表す状態とルールを決め、その後に入力、表示、音、演出へ接続する。
- UIや入力の追加であっても、ゲーム状態を直接操作せず、既存のコマンド経路を通す。
- 音や視覚演出はゲーム結果に付随するcueとして扱い、ルール側に具体的なライブラリ依存を入れない。
- 設定、統計、保存データなどは壊れた値や古い値を受け取っても安全に初期値へ戻せるようにする。
- TypeScriptの型を使って不正な状態や未処理のケースを減らす。`any` や暗黙の文字列分岐は避ける。
- 単純な処理に過剰な抽象化は入れない。ただし、機能追加ごとに同じ場所へ分岐が増え続ける場合は先に構造を分ける。
- 新しいライブラリは、実装効率、表現力、保守性、テスト容易性が上がる場合は採用してよい。採用時は静的配信、初期化コスト、責務分離への影響を確認する。

## テスト方針

- ゲームルール、状態遷移、スコア、連鎖、アイテム効果、モード成否などは単体テストで守る。
- 進行管理は、コマンドごとの状態変化、ゲームオーバー処理、保存タイミング、音や演出cueの生成をテストする。
- 描画ライブラリや音ライブラリの内部挙動は単体テストで深追いしない。主要導線、Canvas描画、入力、レイアウトはE2Eで確認する。
- バグ修正では、先に再現するテストを追加し、そのテストが落ちることを確認してから修正する。
- リファクタリングでは、既存挙動を変えないことをテストで確認する。仕様変更を混ぜる場合は、変更点を明確に分ける。

## 変更後の確認

最低限:

```bash
bun run build
bun run test
```

UI、描画、音、入力、レイアウトを変更した場合:

```bash
bun run test:e2e
```

カバレッジやルール周辺を大きく変更した場合:

```bash
bun run test:coverage
```

公開前には、秘密情報やローカルパスが混入していないことも確認してください。
